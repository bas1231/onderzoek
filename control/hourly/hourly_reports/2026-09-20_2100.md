# Hourly Research Director — 2026-09-20 21:00 CEST

Status: ACTIVE_HOUR / RESEARCH_ONLY / NO_PROVEN_EDGE

## Source coverage

Fresh sweep focused on the 20:00 next-hour questions: free/public Gamma-to-NegRisk mapping, on-chain question/market semantics, and CLOB read-only book linkage. Official Polymarket public API/data documentation, official NegRisk contract docs/code, and current 2026 schema observations were compared against the existing NegRisk findings. Legacy Radar V4 and Proof Trader were not used.

## New evidence

**PM-NR-PUBLIC-MAP-003.** Public Gamma metadata appears sufficient to remove a large part of the prior discovery/indexing blocker. Market records expose `conditionId`, `questionID`, `clobTokenIds`, `negRisk`, and NegRisk-specific market/request IDs; public CLOB endpoints consume token IDs for read-only prices/books. Current 2026 Gamma schema observations additionally expose event-level `negRiskFeeBips` and v2 `onchainEventId`. Official NegRisk operator docs independently establish that market creation carries `_feeBips` and question preparation binds `_marketId` to a distinct `_requestId`.

Durable finding: `knowledge/polymarket/hourly_findings_2026-09-20_2100.md`.

## Known / duplicate / rejected

- Full conversion cashflow (`collateral + complement YES`, fee, exact-one-TRUE/all-FALSE branch) was already established at 20:00 and is not counted as new.
- Public read-only CLOB orderbook access was known generally; the new value is the clearer metadata bridge from Gamma identifiers to the books required for a NegRisk census.
- Third-party indexers remain optional tooling leads, not required dependencies and not authoritative protocol evidence.

## Falsification / negative evidence

Pre-Build Killer: PASS only as a cheap data-acquisition/reconciliation experiment; FAIL as a strategy-build warrant.

Chief Falsifier:
1. **Semantic attack:** `conditionId`, `questionID`, `negRiskRequestID`, `negRiskMarketID/onchainEventId` are not interchangeable. Mapping must preserve identifier type/provenance.
2. **Data attack:** event grouping and current schema fields must be directly sampled/reconciled against live Gamma and, for a validation subset, on-chain preparation events. Secondary client schema is not enough by itself.
3. **Execution attack:** Gamma bestBid/bestAsk cannot establish executable portfolio economics; synchronized full CLOB depth, conversion fee, gas, approvals, inventory and output depth remain mandatory.

Independent Reproducer: not triggered; no positive economic instance exists yet.

## Serious candidates

`PM-NR-V2-CONVERT-001` remains a structural candidate. Its data-acquisition blocker is materially weaker: a zero-cost public census path is now plausible. No economic candidate promoted.

## Blockers

- no completed live enumeration of one current active NegRisk event with all child markets;
- `negRiskFeeBips`/`onchainEventId` need direct live Gamma confirmation for current v2 records;
- no independent reconciliation against `MarketPrepared`/`QuestionPrepared` for a validation subset;
- no synchronized depth-aware portfolio or gas/approval/inventory calculation;
- exact-one-TRUE still requires rule/semantic proof per event.

## Decision

KEEP RESEARCHING the NegRisk structural lane. `NO_PROVEN_EDGE`.

## next_hour

1. Enumerate a current active NegRisk event using only public Gamma and preserve exact raw identifier fields.
2. For every child market, obtain public CLOB full books via `clobTokenIds`; do not use Gamma bestBid/bestAsk as execution evidence.
3. Reconcile a small validation subset against on-chain market/question preparation semantics and fee metadata; explicitly test v1 versus v2 identifier rules.
4. Only if mapping passes, compute the first depth-aware fee-adjusted conversion portfolio as research evidence; still no orders or wallet actions.
5. Continue broad scout coverage but store only gate-changing evidence.
